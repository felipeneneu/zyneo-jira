# Sprint 04 - Copilot Core

**Data**: 2026-01-17
**Escopo**: Task comments, notifications inbox, stale/overdue rules, actionable modal

---

## Mudanças Implementadas

### A) Schema Appwrite (MANUAL)

Documentação criada em `APPWRITE_SCHEMA_UPDATES_SPRINT_04.md`:
- Atualizar `tasks`: `lastActivityAt`, `flags[]`
- Criar collection `task_comments`
- Criar collection `notifications`

### B) Backend

| Rota | Método | Descrição |
|------|--------|-----------|
| `/api/comments?taskId=xxx` | GET | Listar comentários |
| `/api/comments` | POST | Criar comentário |
| `/api/notifications?filter=` | GET | Listar notificações |
| `/api/notifications/:id/read` | POST | Marcar como lida |
| `/api/notifications/:id/star` | POST | Favoritar/desfavoritar |
| `/api/notifications/:id/archive` | POST | Arquivar |

### C) Rule Engine

- Função `checkRulesForWorkspace()` em `src/features/tasks/utils/check-rules.ts`
- Regra STALE: 48h+ sem atividade em IN_PROGRESS/IN_REVIEW
- Regra OVERDUE: dueDate no passado e status != DONE
- Auto-limpa flags e arquiva notificações quando resolvido

### D) UI

- `NotificationsList` refatorado com API real e `MembersAvatar`
- `NotificationModal` acionável com botões contextuais
- `TaskComments` timeline com composer e tipos

### Task Page Comments
- [x] Add "Activity / Updates" section to task page
- [x] Comments timeline component
- [x] Comment composer with type selection
- [x] Display flags (stale/overdue/blocked) in task header

---

## Checklist de Verificação

### Pré-requisitos
- [ ] Criar collections no Appwrite Console (ver `APPWRITE_SCHEMA_UPDATES_SPRINT_04.md`)
- [ ] Adicionar IDs ao `.env.local`:
  ```
  NEXT_PUBLIC_APPWRITE_TASK_COMMENTS_ID=<id>
  NEXT_PUBLIC_APPWRITE_NOTIFICATIONS_ID=<id>
  ```

### Testes Funcionais

1. **Overdue**
   - [ ] Criar task com dueDate de ontem
   - [ ] Abrir lista de tasks → ver badge overdue
   - [ ] Ver notificação na inbox
   - [ ] Clicar em "Replanejar data" → funciona

2. **Stale**
   - [ ] Colocar task em IN_PROGRESS
   - [ ] Setar lastActivityAt para 3 dias atrás (via Appwrite Console)
   - [ ] Ver badge stale na task
   - [ ] Ver notificação na inbox
   - [ ] Clicar "Estou trabalhando" → comentário criado, stale limpo

3. **Mentions**
   - [ ] Criar comentário com @[Nome](memberId)
   - [ ] Verificar que usuário mencionado recebe notificação

4. **Inbox**
   - [ ] Filtro "Não lidas" funciona
   - [ ] Filtro "Favoritas" funciona
   - [ ] Star/Archive funcionam
   - [ ] Modal abre com ações corretas

---

## Arquivos Criados/Modificados

### Novos
- `src/features/comments/` (types, schemas, server/route, api hooks, components)
- `src/features/notifications/` (types, schemas, server/route, api hooks, utils)
- `src/features/tasks/utils/check-rules.ts`
- `APPWRITE_SCHEMA_UPDATES_SPRINT_04.md`

### Modificados
- `src/config.ts` - novos IDs de collections
- `src/app/api/[[...route]]/route.ts` - novas rotas
- `src/features/tasks/types.ts` - campos `lastActivityAt`, `flags`
- `src/features/notification/components/` - refatorados para API real
